arr = [20, 12, `okay`, true];
arr.splice(2, 1, "hello", `world`); //splice  method is used to change the contents of an array by removing or replacing existing elements and/or adding
console.log(arr);
